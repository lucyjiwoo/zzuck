services:

  # ─────────────────────────────────────────
  # PostgreSQL + pgvector
  # ─────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    container_name: zzuck-postgres
    environment:
      POSTGRES_DB: zzuck
      POSTGRES_USER: zzuck
      POSTGRES_PASSWORD: zzuck
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U zzuck -d zzuck"]
      interval: 5s
      timeout: 5s
      retries: 5

  # ─────────────────────────────────────────
  # LocalStack (SQS + S3)
  # ─────────────────────────────────────────
  localstack:
    image: localstack/localstack:3
    container_name: zzuck-localstack
    environment:
      SERVICES: sqs,s3
      DEFAULT_REGION: us-east-1
      # Init script runs automatically on startup
      LOCALSTACK_VOLUME_DIR: /var/lib/localstack
    ports:
      - "4566:4566"
    volumes:
      - ./localstack/init:/etc/localstack/init/ready.d
      - localstack_data:/var/lib/localstack
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:4566/_localstack/health | grep -q '\"sqs\": \"running\"'"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
  localstack_data:
